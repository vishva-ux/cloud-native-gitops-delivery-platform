apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  labels:
    app: {{ .Release.Name }}-postgres
spec:
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5432
      name: postgres
  selector:
    app: {{ .Release.Name }}-postgres
  type: {{ .Values.service.type }}
